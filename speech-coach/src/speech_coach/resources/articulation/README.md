# 조음 단면도 리소스

- `diagram_regions.json` — overlay 영역 (`overlay_pct`는 이미지 대비 %)
- `articulation-diagram.png` — 단면도 원본 (프론트 `public/articulation-diagram.png` 에도 동일 파일 복사)

프론트는 `GET /v1/articulation/diagram` 또는 analyze 응답의 `feedback.diagram` 사용.
